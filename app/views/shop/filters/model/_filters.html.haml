.mb
	%h3= select_title
	%h4= t('products', count: results.length)
.inline-center.tb
	= selected_link('all_models', shop_products_path(kind: params[:kind]))
	= '|'
	= selected_link('man', shop_products_path(kind: params[:kind], customer: 'man'))
	= '|'
	= selected_link('woman', shop_products_path(kind: params[:kind], customer: 'woman'))
	= '|'
	= selected_link('boy', shop_products_path(kind: params[:kind], customer: 'boy'))
	= '|'
	= selected_link('girl', shop_products_path(kind: params[:kind], customer: 'girl'))
.right
	.mb
		= render partial: 'shop/filters/equalizer'
	.tb
		%h5= t('products', count: results.length)
