#thread
	= link_to t_scoped(:products), admin_products_path(), class: "btn"
	= link_to t_scoped(:galleries), galleries_path(), class: "btn"
	= link_to t_scoped(:users), users_path(), class: "btn"
	= link_to t_scoped(:posters), products_path(customer: "child"), class: "btn"
	= link_to t_scoped(:proposals), products_path(kind: "material"), class: "btn"